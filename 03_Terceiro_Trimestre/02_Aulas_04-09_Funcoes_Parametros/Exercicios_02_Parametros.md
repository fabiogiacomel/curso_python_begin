# Exercícios 02: Parâmetros

---

## Série A: Reprodução
**A1.** Crie `meus_parametros.py`.
**A2.** Copie e execute:
```python
def dobro(numero):
    res = numero * 2
    print(f"O dobro de {numero} é {res}")

dobro(10)
dobro(5)
```

---

## Série B: Modificação (Triplo)
**B1.** Mude a função para calcular o **Triplo** (vezes 3).
**B2.** Chame a função passando o número 100.

---

## Série C: Criação (Tabuada Personalizada)
**C1.** Crie uma função chamada `gerar_tabuada(numero)`.
**C2.** Dentro dela, use um `for` (aprendido no 2º Tri) para imprimir a tabuada desse número (do 1 ao 10).
**C3.** Teste chamando `gerar_tabuada(5)` e `gerar_tabuada(9)`.

---

## Série D: Desafio (Calculadora Completa)
**D1.** Crie uma função `calcular(n1, n2, operacao)`.
**D2.** Dentro dela, use `if/elif` para verificar a `operacao` ("soma", "sub", "mult", "div").
**D3.** Imprima o resultado certo baseada na operação e nos números.
**D4.** Teste com: `calcular(10, 2, "soma")` e `calcular(20, 5, "div")`.
