# Avaliação Final: Python 1

**Nome:** __________________________________________________

---

## Parte 1: Múltipla Escolha (4.0 pontos)

**1. Qual biblioteca usamos para calcular raiz quadrada?**
( ) `import random`
( ) `import math`
( ) `import root`

**2. Como definimos uma função nova?**
( ) `function somar():`
( ) `def somar():`
( ) `create somar():`

**3. O que `"   abc   ".strip()` faz?**
( ) Deixa maiúsculo ("ABC")
( ) Remove os espaços das pontas ("abc")
( ) Divide o texto em listas.

**4. Se `lista = [10, 20, 30]`, o que `len(lista)` devolve?**
( ) 30
( ) 60
( ) 3

---

## Parte 2: Desafio de Código (6.0 pontos)

**Situação:** Você foi contratado para criar um validador de senhas.
**Regras:**
1.  A senha deve ter mais de 6 caracteres.
2.  A senha NÃO pode ser igual ao nome do usuário.
3.  O programa deve limpar os espaços antes de verificar.

**Escreva uma função `validar_senha(usuario, senha)` que retorne True ou False.**
