# Exercícios 05 e 06: Variáveis e Tipos

---

## Série A: Reprodução
**A1.** Crie `meu_perfil.py`.
**A2.** Copie, complete com SEUS dados e execute:
```python
nome = "Seu Nome Aqui" # str
idade = 15             # int
altura = 1.70          # float
gosta_de_python = True # bool

print("Nome:", nome)
print(type(nome))

print("Idade:", idade)
print(type(idade))
```

---

## Série B: Modificação
**B1.** No código acima, mude a idade para `15.5`.
**B2.** Execute. O tipo mudou para `int` ou `float`?

---

## Série C: Criação
**C1.** Crie um programa para uma **Loja de Carros**.
Crie variáveis para guardar:
-   Modelo do carro (ex: "Fusca")
-   Ano de fabricação (ex: 1980)
-   Preço (ex: 15000.00)
-   É novo? (True ou False)

No final, use `print()` para mostrar todas as informações.

---

## Série D: Desafio (Tipos)
**D1.** Responda sem rodar (anote no caderno): Qual o tipo das variáveis abaixo?

a) `x = "100"` -> Tipo?
b) `y = 100`   -> Tipo?
c) `z = 100.0` -> Tipo?
d) `w = False` -> Tipo?

**D2.** O que acontece se eu tentar somar `x + y`?
(Tente pensar primeiro, depois teste no computador: `print(x + y)`).
Você vai descobrir um erro novo: `TypeError`.
O Python não soma Texto com Número!
